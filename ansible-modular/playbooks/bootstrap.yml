---
- name: Bootstrap new hosts (initial setup + connection)
  hosts: all:!hq
  become: true
  gather_facts: true
  vars:
    ansible_user: "{{ bootstrap_user | default('root') }}"
  roles:
    - bootstrap
