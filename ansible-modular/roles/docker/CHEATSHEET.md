# docker — шпаргалка

⚠️ Роль **пустая** (scaffold). В `tasks/main.yml` только комментарий TODO —
никаких таск нет, роль ничего не делает.

Планировалось: установка Docker. Но k3s использует свой containerd, поэтому
отдельный Docker, скорее всего, не нужен. Если таск спросят «зачем» — их пока
нет, роль не реализована.
