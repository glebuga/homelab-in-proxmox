# etcd — шпаргалка

⚠️ Роль **пустая** (scaffold). В `tasks/main.yml` только комментарий TODO —
никаких таск нет, роль ничего не делает.

Планировалось: внешний etcd. Но k3s уже содержит встроенный etcd, отдельный
не обязателен. Если таск спросят «зачем» — их пока нет, роль не реализована.
