# patroni — шпаргалка

⚠️ Роль **пустая** (scaffold). В `tasks/main.yml` только комментарий TODO —
никаких таск нет, роль ничего не делает.

Планировалось: PostgreSQL HA через Patroni. Таски не написаны.
Если таск спросят «зачем» — их пока нет, роль не реализована.
